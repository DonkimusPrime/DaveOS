# CLAUDE.md — Travel
Scope: trip planning and logistics. One subfolder per trip in `Trips/` with a brief from `91_Templates/trip-brief.md`.
Rules:
- Check the connected calendar before proposing dates.
- Preferences in `MEMORY.md` apply to every trip unless the brief overrides them.
- Never book or purchase; produce a ready-to-execute plan.
