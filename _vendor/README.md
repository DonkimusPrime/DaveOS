# _vendor — pristine third-party installs (read-only, enforced by hook)

`cowork-academy/` holds Jeff Su's installed system exactly as it arrived. Never edit here.
The merge copies from here into DaveOS workstations; `MERGE-LOG.md` records every file's origin.
Keep this repo private — these files are licensed for your use.
When he ships an update, drop the new version in a dated sibling folder (`cowork-academy-2026-10/`) and diff.
